# Purpose
Unit test for `buildServerStartEvent` to ensure the `server_start` event payload includes required fields with expected values.

# Key Logic
- Constructs a minimal config object and asserts the event fields.

# Interactions
- Imports from `src/logger.js`.

# Refs
Refs: decision structured-logging-with-slog-guidelines

